Archived code that is not part of the active NetVisor MVP runtime.

Keep this folder only as a temporary reference while migrating.

Active code lives in:
- `agent/`
- `frontend/`
- `netvisor/backend/app/`
- `netvisor/gateway/`

Nothing under `legacy/` should be imported by active runtime code or tests.
